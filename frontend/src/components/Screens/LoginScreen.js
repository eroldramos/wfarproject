import React, { Fragment } from "react";
import Login from "../Authentications/Login";
const LoginScreen = () => {
  return (
    <Fragment>
      <Login />
    </Fragment>
  );
};

export default LoginScreen;
